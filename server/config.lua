FORM_WEBHOOKS = {
    MISSION_ROW = "WEBHOOK_URL",
    PILLBOX_HOSPITAL = "WEBHOOK_URL",
}

-- Used for pre-filling the contact input, as ESX doesn't have phone number field in it's core, you'll need to adjust it to your likings.
ESX_GET_PLAYER_CONTACT = function(identifier)
    -- do your magic here
end